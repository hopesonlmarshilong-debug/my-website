name: CI/CD Pipelineon:
  push:
    branches: [ "main" ]jobs:
  build:
    runs-on: ubuntu-latestjavascriptsteps:
- name: Checkout code
  uses: actions/checkout@v3

- name: Install {{IE_0}}http://Node.js{{/IE_0}}
  uses: actions/setup-node@v3
  with:
    node-version: '18'

- name: Install dependencies
  run: npm install

- name: Run tests
  run: npm test

- name: Build project
  run: npm run build